# Managed by the cloud_host role. Local users keep the host kvm-group policy;
# only the containerized libvirt QEMU identity receives an additional ACL.
KERNEL=="kvm", GROUP="kvm", MODE="0660", RUN+="/usr/bin/setfacl -m u:{{ cloud_libvirt_qemu_uid }}:rw /dev/kvm"
