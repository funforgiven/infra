apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: zitadel-postgres-backup
  namespace: identity
spec:
  retentionPolicy: 30d
  configuration:
    destinationPath: s3://fahrican-cloud-recovery/undercloud/zitadel/
    endpointURL: https://s3.us-west-004.backblazeb2.com
    s3Credentials:
      accessKeyId:
        name: zitadel-b2-backup
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: zitadel-b2-backup
        key: ACCESS_SECRET_KEY
      region:
        name: zitadel-b2-backup
        key: AWS_REGION
    wal:
      compression: gzip
      encryption: AES256
      maxParallel: 2
    data:
      compression: gzip
      encryption: AES256
      jobs: 2
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: zitadel-postgres-daily
  namespace: identity
spec:
  schedule: "0 17 2 * * *"
  backupOwnerReference: self
  cluster:
    name: zitadel-postgres
  immediate: true
  method: plugin
  online: true
  onlineConfiguration:
    waitForArchive: true
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
    parameters:
      barmanObjectName: zitadel-postgres-backup
  target: prefer-standby
