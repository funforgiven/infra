version: 1
resources:
  - resource: openstack-projects-networks-compute-and-block-storage
    owner: opentofu
    state: OpenTofu state and reviewed configuration in Git
  - resource: openstack-shared-filesystem-quotas
    owner: pinned-api-reconciler
    state: Git-declared Manila limits reconciled through the scoped OpenStack administration credential
  - resource: magnum-services-cluster-and-flux-bootstrap
    owner: pinned-api-reconciler
    state: Desired topology in Git; kubeconfig exists only on a memory-backed reconciliation volume
  - resource: aws-mail-vpc-compute-rds-s3-secrets-and-monitoring
    owner: opentofu
    state: OpenTofu state and reviewed configuration in Git; runtime secret versions are produced only by RDS, the appliance IAM role, or the no-echo enrollment boundary
  - resource: public-and-private-dns
    owner: opentofu
    state: OpenTofu state and reviewed DNS records in Git
  - resource: zitadel-projects-applications-and-roles
    owner: opentofu
    state: OpenTofu state and reviewed identity configuration in Git
  - resource: openai-hermes-runtime-key
    owner: sops
    state: Operator-issued project key stored only as admin-recipient SOPS ciphertext and a mode-0400 runtime file
  - resource: resend-domain-and-verification
    owner: pinned-api-reconciler
    state: Desired domain policy and non-secret identifiers in Git
  - resource: kubernetes-resources
    owner: flux
    state: Flux Kustomizations and SOPS ciphertext in Git
  - resource: nixos-hosts-and-system-services
    owner: nixos
    state: NixOS closures built from the repository flake
  - resource: home-assistant-os-appliance
    owner: opentofu
    state: Pinned official HAOS image and VM attachment in OpenTofu; unavoidable appliance state is registered, encrypted, and restore-tested under the manual exception policy
  - resource: application-api-configuration
    owner: pinned-api-reconciler
    state: Desired application configuration and comparison tests in Git
  - resource: runtime-secrets
    owner: sops
    state: SOPS ciphertext in Git and mode-0400 runtime materialization
  - resource: monitoring-alerting-and-backups
    owner: flux
    state: Flux resources for Kubernetes and NixOS modules for standalone hosts
  - resource: services-and-host-offsite-b2-prefixes
    owner: pinned-api-reconciler
    state: Existing retained Backblaze B2 bucket, Git-managed lifecycle and prefix policy, ephemeral master bootstrap, and one independently scoped SOPS credential per writer
  - resource: telegram-bot-metadata-and-private-targets
    owner: pinned-api-reconciler
    state: Public bot metadata and target-selection rules in Git; bot tokens and discovered numeric IDs are independently routed SOPS ciphertext
