# Activate Zigbee only after assigning the coordinator a fixed IoT address.
ZIGBEE_ENABLED=false
ZIGBEE_SERIAL_PORT=tcp://unconfigured.invalid:6638
ZIGBEE_CHANNEL=20
# OTBR is deployed separately with replicas: 0 until its RCP is configured.
